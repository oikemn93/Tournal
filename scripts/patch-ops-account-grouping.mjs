import fs from 'node:fs';
const op='src/lib/ops.ts';
let o=fs.readFileSync(op,'utf8');
if(!o.includes('export async function createOpsAccount(')) o += `\nexport async function createOpsAccount(name:string) {\n  const rows=await opsDataRequest<OpsAccount[]>("ops_accounts?select=*",{method:"POST",headers:{Prefer:"return=representation"},body:JSON.stringify({name:name.trim(),stage:"prospect",health_status:"unknown"})});\n  if(!rows[0]) throw new Error("Compte client non créé"); return rows[0];\n}\n\nexport async function linkOpsBoutiqueToAccount(boutiqueId:string,accountId:string) {\n  const rows=await opsDataRequest<OpsAccountBoutique[]>("ops_account_boutiques?on_conflict=boutique_id&select=*",{method:"POST",headers:{Prefer:"resolution=merge-duplicates,return=representation"},body:JSON.stringify({boutique_id:boutiqueId,account_id:accountId})});\n  if(!rows[0]) throw new Error("Liaison boutique-compte non modifiée"); return rows[0];\n}\n`;
fs.writeFileSync(op,o);
const p='src/app/components/TournalOpsWorkspace.tsx';
let s=fs.readFileSync(p,'utf8');
s=s.replace('createOpsContact, createOpsInteraction, createOpsTask, createOpsTicket, decideOpsAccessRequest,', 'createOpsAccount, createOpsContact, createOpsInteraction, createOpsTask, createOpsTicket, decideOpsAccessRequest,');
s=s.replace('loadOpsSupportDiagnostic, loadOpsWorkspace, requestOpsBoutiqueAccess, updateOpsAccount, updateOpsContact, updateOpsOnboarding,', 'loadOpsSupportDiagnostic, loadOpsWorkspace, linkOpsBoutiqueToAccount, requestOpsBoutiqueAccess, updateOpsAccount, updateOpsContact, updateOpsOnboarding,');
s=s.replace('const [contactName,setContactName]=useState("");', 'const [accountGroup,setAccountGroup]=useState(""); const [newAccountName,setNewAccountName]=useState(""); const [accountGroupingSaving,setAccountGroupingSaving]=useState(false);\n  const [contactName,setContactName]=useState("");');
s=s.replace('const canManageContacts=canSystemAdmin||["sales","service","support","manager"].includes(opsRole??"");', 'const canManageContacts=canSystemAdmin||["sales","service","support","manager"].includes(opsRole??"");\n  const canGroupAccounts=canSystemAdmin||opsRole==="manager";');
const anchor='<div className="mt-4"><div className="flex items-center justify-between"><p className="text-xs font-black">Contacts</p>';
const block=`{canGroupAccounts&&<div className="mt-4 rounded-xl border border-dashed p-3"><p className="text-xs font-black">Organisation multi-boutiques</p><p className="text-[11px] text-slate-500 mt-0.5">Action explicite uniquement : aucune fusion automatique des clients.</p><div className="mt-2 grid sm:grid-cols-[1fr_auto] gap-2"><select value={accountGroup} onChange={e=>setAccountGroup(e.target.value)} className={input}><option value="">Choisir un compte existant</option>{(workspace?.accounts??[]).filter(a=>a.id!==selectedAccount.id).map(a=><option key={a.id} value={a.id}>{a.name}</option>)}</select><button disabled={!accountGroup||accountGroupingSaving} onClick={async()=>{if(!accountGroup)return;setAccountGroupingSaving(true);try{const link=await linkOpsBoutiqueToAccount(selected.id,accountGroup);setWorkspace(w=>w?{...w,accountBoutiques:[...w.accountBoutiques.filter(x=>x.boutique_id!==selected.id),link]}:w);setAccountGroup("");}catch(err){setError(err instanceof Error?err.message:"Regroupement impossible")}finally{setAccountGroupingSaving(false)}}} className="rounded-xl bg-slate-950 text-white px-3 py-2 text-xs font-black disabled:opacity-50">Rattacher</button></div><div className="mt-2 grid sm:grid-cols-[1fr_auto] gap-2"><input value={newAccountName} onChange={e=>setNewAccountName(e.target.value)} placeholder="Nouveau compte / organisation" className={input}/><button disabled={!newAccountName.trim()||accountGroupingSaving} onClick={async()=>{setAccountGroupingSaving(true);try{const account=await createOpsAccount(newAccountName);const link=await linkOpsBoutiqueToAccount(selected.id,account.id);setWorkspace(w=>w?{...w,accounts:[account,...w.accounts],accountBoutiques:[...w.accountBoutiques.filter(x=>x.boutique_id!==selected.id),link]}:w);setNewAccountName("");}catch(err){setError(err instanceof Error?err.message:"Création du compte impossible")}finally{setAccountGroupingSaving(false)}}} className="rounded-xl bg-slate-100 px-3 py-2 text-xs font-black disabled:opacity-50">Créer & rattacher</button></div></div>}<div className="mt-4"><div className="flex items-center justify-between"><p className="text-xs font-black">Contacts</p>`;
s=s.replace(anchor,block);
fs.writeFileSync(p,s);
