import fs from 'node:fs';

const posPath = 'src/app/screens/POSView.tsx';
let pos = fs.readFileSync(posPath, 'utf8');

const oldAccess = `  const activeAssignment = currentUser.assignments.find(assignment => assignment.boutiqueId === boutique.id);\n  const canManageAnyPendingOrder = currentUser.isSuperAdmin || activeAssignment?.role === \"Propriétaire\";\n  const canManagePendingOrder = (invoice: Invoice) => canManageAnyPendingOrder || invoice.operatorId === currentUser.id;\n  const canEditPendingOrder = (invoice: Invoice) => canManagePendingOrder(invoice) && invoice.origin !== \"client_profile\";\n  const canCancelOrder = (invoice: Invoice) => canCancelPendingOrder && canManagePendingOrder(invoice) && invoice.origin !== \"client_profile\";`;
const newAccess = `  const activeAssignment = currentUser.assignments.find(assignment => assignment.boutiqueId === boutique.id);\n  const isBoutiqueOwner = activeAssignment?.role === \"Propriétaire\" || activeAssignment?.role === \"owner\";\n  const canManageAnyPendingOrder = currentUser.isSuperAdmin || isBoutiqueOwner;\n  const canManagePendingOrder = (invoice: Invoice) => canManageAnyPendingOrder || invoice.operatorId === currentUser.id;\n  const canEditPendingOrder = (invoice: Invoice) => canManagePendingOrder(invoice) && invoice.origin !== \"client_profile\";\n  const hasCancelPermission = canCancelPendingOrder || currentUser.isSuperAdmin || isBoutiqueOwner || !!activeAssignment?.droits?.annulation_commande;\n  const canCancelOrder = (invoice: Invoice) => hasCancelPermission && canManagePendingOrder(invoice) && invoice.origin !== \"client_profile\";`;
if (!pos.includes(oldAccess)) throw new Error('POS access block changed');
pos = pos.replace(oldAccess, newAccess);

const oldQtyFn = `  function updateCartQty(lineIndex: number, newDispQty: number) {\n    if (newDispQty <= 0) { removeFromCart(lineIndex); return; }\n    setCart(prev => prev.map((item, index) => {\n      if (index !== lineIndex) return item;\n      if (item.sellUnit && item.sellQty !== undefined) {\n        const p = products.find(pr => pr.id === item.productId);\n        const newBase = p ? toBaseQty(newDispQty, item.sellUnit, p) : newDispQty;\n        return { ...item, sellQty: newDispQty, qty: newBase };\n      }\n      return { ...item, qty: newDispQty };\n    }));\n  }`;
const newQtyFn = `${oldQtyFn}\n  function updateCartPrice(lineIndex: number, newPrice: number) {\n    if (!Number.isFinite(newPrice) || newPrice < 0) return;\n    setCart(prev => prev.map((item, index) => index === lineIndex ? { ...item, prixUnit:newPrice } : item));\n  }`;
if (!pos.includes(oldQtyFn)) throw new Error('POS quantity updater changed');
pos = pos.replace(oldQtyFn, newQtyFn);

const oldPrice = `<span className=\"text-xs text-muted-foreground ml-0.5\">{dUnit} × {fmt(item.prixUnit)}</span>`;
const newPrice = `{editingInvoice ? (\n                        <label className=\"ml-1 flex items-center gap-1 text-xs text-muted-foreground\">\n                          {dUnit} ×\n                          <input\n                            type=\"number\"\n                            min=\"0\"\n                            step=\"1\"\n                            value={item.prixUnit}\n                            onChange={event=>updateCartPrice(lineIndex, Number(event.target.value))}\n                            className=\"w-24 rounded-lg border border-border bg-background px-2 py-1 text-right text-xs font-black text-foreground\"\n                            aria-label={\`Prix unitaire de \${item.nom}\`}\n                          />\n                        </label>\n                      ) : (\n                        <span className=\"text-xs text-muted-foreground ml-0.5\">{dUnit} × {fmt(item.prixUnit)}</span>\n                      )}`;
if (!pos.includes(oldPrice)) throw new Error('POS checkout price label changed');
pos = pos.replace(oldPrice, newPrice);

fs.writeFileSync(posPath, pos);

const migration = `-- Align sale stock lifecycle with business rules:\n-- * counter/POS: stock only after full payment; partial payment is forbidden\n-- * registered Clients flow: stock is committed automatically when the order is created\n-- Existing historical rows are not rewritten.\n\nbegin;\n\ncreate or replace function private.commit_pos_stock_after_invoice_line()\nreturns trigger\nlanguage plpgsql\nsecurity definer\nset search_path = pg_catalog, public, private\nas $$\ndeclare\n  v_invoice public.invoices%rowtype;\nbegin\n  select * into v_invoice from public.invoices\n  where boutique_id = new.boutique_id and id = new.invoice_id;\n\n  if found\n     and lower(coalesce(v_invoice.type,'')) <> 'retour'\n     and coalesce(v_invoice.origin,'pos') = 'client_profile'\n     and v_invoice.status <> 'annulée'\n     and v_invoice.stock_deducted_at is null then\n    perform private.commit_invoice_stock(new.boutique_id,new.invoice_id,now(),auth.uid(),false);\n  end if;\n  return null;\nend;\n$$;\n\nrevoke all on function private.commit_pos_stock_after_invoice_line() from public, anon, authenticated;\n\n-- POS orders created before this migration may already have immediate stock\n-- commitment. Their existing update/cancel RPCs keep the compatibility release\n-- path, while new POS orders remain uncommitted until full collection.\n\n-- Strengthen full-payment semantics for counter invoices at the database boundary.\ndo $do$\ndeclare\n  v_def text;\n  v_old text := 'if coalesce(v_invoice.origin,''pos'')=''pos'' and abs(v_requested-v_remaining)>v_epsilon and coalesce(current_setting(''tournal.pos_full_split'',true),'''')<>''on'' then raise exception ''counter sale must be paid in full''; end if;';\nbegin\n  select pg_get_functiondef('public.record_payment(text,text,uuid,numeric,text)'::regprocedure) into v_def;\n  if position(v_old in v_def)=0 then raise exception 'record_payment full-payment guard shape changed'; end if;\n  -- Guard already expresses the intended rule; re-executing the canonical\n  -- definition here makes the dependency explicit in this migration.\n  execute v_def;\nend\n$do$;\n\n-- Keep direct table writes closed; mutations must pass through authenticated RPCs.\ndo $do$\nbegin\n  if exists (\n    select 1 from pg_policies\n    where schemaname='public'\n      and tablename in ('invoices','invoice_lines','stock_entries')\n      and cmd in ('INSERT','UPDATE','DELETE','ALL')\n      and 'authenticated'=any(roles)\n  ) then\n    raise exception 'unexpected authenticated write policy on protected sale/stock tables';\n  end if;\nend\n$do$;\n\ncommit;\n`;
fs.writeFileSync('supabase/migrations/20260902224500_align_pos_client_stock_lifecycle.sql', migration);
