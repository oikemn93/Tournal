name: Apply inventory variance cache
on:
  push:
    branches: [main]
    paths: [.github/workflows/apply-inventory-variance-cache.yml]
permissions:
  contents: write
jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 10
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm
      - run: python tools/patch_inventory_variance_cache.py
      - run: pnpm install --frozen-lockfile
      - run: pnpm build
      - run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add src/app/screens/InventoryView.tsx
          git diff --cached --quiet || git commit -m "fix: align inventory adjustment cache with FIFO variance"
          git push
