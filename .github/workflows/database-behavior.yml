name: Database Behavior

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  database-behavior:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4

      - uses: supabase/setup-cli@v1
        with:
          version: 2.116.0

      - name: Initialize isolated local Supabase
        run: |
          mkdir -p .ci-historical-migrations
          mv supabase/migrations/*.sql .ci-historical-migrations/
          supabase init --force
          supabase db start

      - name: Load canonical CI baseline
        run: psql 'postgresql://postgres:postgres@127.0.0.1:54322/postgres' -v ON_ERROR_STOP=1 -f scripts/ci-db-baseline.sql

      - name: Apply candidate migrations
        run: |
          psql 'postgresql://postgres:postgres@127.0.0.1:54322/postgres' -v ON_ERROR_STOP=1 -f .ci-historical-migrations/20260904183500_consolidate_disbursement_guards.sql
          psql 'postgresql://postgres:postgres@127.0.0.1:54322/postgres' -v ON_ERROR_STOP=1 -f .ci-historical-migrations/20260904184500_make_read_rpcs_session_independent.sql
          psql 'postgresql://postgres:postgres@127.0.0.1:54322/postgres' -v ON_ERROR_STOP=1 -f .ci-historical-migrations/20260904184600_make_inventory_reads_session_independent.sql
          # The compact behavior baseline predates transfer payment history, but
          # the production dashboard schema already contains this table. Mirror
          # only the columns needed by the current dashboard before applying its
          # candidate definition; the canonical replay still validates the full schema.
          psql 'postgresql://postgres:postgres@127.0.0.1:54322/postgres' -v ON_ERROR_STOP=1 -c "create table if not exists public.transfer_charge_payments (id bigint primary key, boutique_id text not null, transfer_id uuid not null, charge_id bigint not null, amount numeric not null check (amount > 0), payment_method text not null, paid_at timestamptz not null default now(), operator_id uuid, operator_name text not null, idempotency_key uuid not null, unique (boutique_id, idempotency_key));"
          psql 'postgresql://postgres:postgres@127.0.0.1:54322/postgres' -v ON_ERROR_STOP=1 -f .github/audit/replay-migrations/20260906181500_stock_integrity_guardrails.sql
          psql 'postgresql://postgres:postgres@127.0.0.1:54322/postgres' -v ON_ERROR_STOP=1 -f .github/audit/replay-migrations/20260906191745_inventory_race_same_transaction_guard.sql
          psql 'postgresql://postgres:postgres@127.0.0.1:54322/postgres' -v ON_ERROR_STOP=1 -f .github/audit/replay-migrations/20260906192500_dashboard_fifo_stock_value.sql

      - name: Run behavioral database matrix
        run: psql 'postgresql://postgres:postgres@127.0.0.1:54322/postgres' -v ON_ERROR_STOP=1 -f scripts/test-db-behavior.sql

      - name: Run stock integrity matrix
        run: psql 'postgresql://postgres:postgres@127.0.0.1:54322/postgres' -v ON_ERROR_STOP=1 -f scripts/test-stock-integrity-db.sql

      - name: Run inventory race matrix
        run: psql 'postgresql://postgres:postgres@127.0.0.1:54322/postgres' -v ON_ERROR_STOP=1 -f scripts/test-inventory-race-db.sql

      - name: Run commercial transfer stock matrix
        run: psql 'postgresql://postgres:postgres@127.0.0.1:54322/postgres' -v ON_ERROR_STOP=1 -f scripts/test-commercial-transfer-stock-db.sql

      - name: Run dashboard FIFO matrix
        run: psql 'postgresql://postgres:postgres@127.0.0.1:54322/postgres' -v ON_ERROR_STOP=1 -f scripts/test-dashboard-fifo-db.sql

      - name: Verify deterministic return lock migration
        run: |
          psql 'postgresql://postgres:postgres@127.0.0.1:54322/postgres' -v ON_ERROR_STOP=1 -f scripts/test-return-lock-order-db.sql
          psql 'postgresql://postgres:postgres@127.0.0.1:54322/postgres' -v ON_ERROR_STOP=1 -f .ci-historical-migrations/20260905175824_order_return_product_locks.sql
          psql 'postgresql://postgres:postgres@127.0.0.1:54322/postgres' -v ON_ERROR_STOP=1 -f .ci-historical-migrations/20260905175824_order_return_product_locks.sql
          psql 'postgresql://postgres:postgres@127.0.0.1:54322/postgres' -v ON_ERROR_STOP=1 -f scripts/test-return-lock-order-db.sql -v assert_patched=1

      - name: Verify Ops RLS and foreign-key indexes
        run: |
          psql 'postgresql://postgres:postgres@127.0.0.1:54322/postgres' -v ON_ERROR_STOP=1 -f scripts/test-ops-rls-index-db.sql
          psql 'postgresql://postgres:postgres@127.0.0.1:54322/postgres' -v ON_ERROR_STOP=1 -f .ci-historical-migrations/20260905175840_optimize_ops_rls_and_foreign_keys.sql
          psql 'postgresql://postgres:postgres@127.0.0.1:54322/postgres' -v ON_ERROR_STOP=1 -f .ci-historical-migrations/20260905175840_optimize_ops_rls_and_foreign_keys.sql
          psql 'postgresql://postgres:postgres@127.0.0.1:54322/postgres' -v ON_ERROR_STOP=1 -f scripts/test-ops-rls-index-db.sql -v assert_patched=1

      - name: Stop local database
        if: always()
        run: supabase stop --no-backup
