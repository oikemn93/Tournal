name: One-shot Ops support access
on:
  push:
    branches: [main]
    paths:
      - .github/workflows/one-shot-ops-support-access.yml
permissions:
  contents: write
jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with: { fetch-depth: 0 }
      - uses: pnpm/action-setup@v4
        with: { version: 10, run_install: false }
      - uses: actions/setup-node@v4
        with: { node-version: 22, cache: pnpm }
      - run: node scripts/patch-ops-support-access.mjs
      - run: pnpm install --frozen-lockfile
      - run: pnpm build
      - name: Commit
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add src/lib/ops.ts src/app/components/TournalOpsWorkspace.tsx
          git commit -m 'feat: add scoped temporary support diagnostics'
          git push origin HEAD:main
