name: Apply transfer directory and conditioning patch

on:
  push:
    branches: [main]
    paths:
      - tools/patch_transfer_directory_multi_conditioning.py
      - .github/workflows/apply-transfer-directory-multi-conditioning.yml

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 10
          run_install: false
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
      - name: Apply patch
        run: python tools/patch_transfer_directory_multi_conditioning.py
      - name: Install locked dependencies
        run: pnpm install --frozen-lockfile
      - name: Build
        run: pnpm build
      - name: Commit functional patch
        shell: bash
        run: |
          set -euo pipefail
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add src/lib/api.ts src/app/screens/TransfersView.tsx
          git diff --cached --quiet || git commit -m "fix: enforce transfer directory and mixed conditioning"
          git push
